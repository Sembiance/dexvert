"use strict";
const XU = require("@sembiance/xu");

exports.meta =
{
	name        : "KwikDraw Drawing",
	ext         : [".kwk"],
	magic       : ["KwikDraw drawing"],
	unsupported : true,
	notes       : "A windows 'object oriented' drawing program. Don't think it was very popular. sandbox/app/KDRAW121.ZIP has the app, works in Win2k, no export ability. Could add a virtual printer driver and then use that to output as PNG."
};
