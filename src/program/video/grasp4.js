/*
import {Program} from "../../Program.js";

export class grasp4 extends Program
{
	website = "https://winworldpc.com/product/fantavision/1x-dos";
	unsafe = true;
}
*/

/*
"use strict";
const XU = require("@sembiance/xu"),
	path = require("path");

exports.meta =
{
	website : "https://winworldpc.com/product/fantavision/1x-dos",
	unsafe  : true
};

exports.dos = () => "GRASP4.EXE";
exports.args = (state, p, r, inPath=state.input.filePath) => ([inPath]);
// A lot of GL files are just loops, but we can't determine that for sure. A lot of others are actually long
exports.dosData = state => ({timeout : XU.MINUTE*4, video : path.join(state.output.absolute, `${state.input.name}.mp4`)});
*/
