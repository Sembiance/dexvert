/--------------\
| TOP Priority |
\--------------/
* Finish up adding all the 'archive' files.
  - Use this instead of DOSBox: https://github.com/copy/v86  (If that is awesome, offer to donate (see the github page))

* Move xutil:archiveUtil to legacy and update all my code to use dexvert.process() instead.
  Also, at this point I should also move xutil:dosUtil to legacy and update anything that uses it in favor of the new @sembiance/DOS

* Add process support for 'pcx' next. This is when I should add program support for deark, nconvert, recoil2png, convert, ffmpeg, etc
  Ensure every program can work just by being called Ensure  abydos 0.2.0

* Add PNG support. It will automatically mark itself as processed so long as the png.inputMeta() (or image.inputMeta()) meta info came back what I expect. Update process.updateProcessedFlag() to call p.formats[state.id.family][state.id.formatid].updateProcessed() if it is present.

* Now add all the rest of the 'image' files

* Add a document next, something that uses unoconv to convert. Create an init.d folder and unoconv-server and ensure it daemonizes and automatically will restart it if the process crashes. Make sure if I use daemonize that I include it as a gentoo package that I need to have installed

* Add all the previously 'supported' file formats

>>> For every new program:
  - If a custom ebuild, move to dexvert and upgrade to latest version (ancient, gameextractor, etc)
    Make sure to save a copy of the files in DISTFILES just in case
	If something that is updated, add it to system_update.txt Less Often section to check for new updates in the future
  - If a custom C program, make a custom native node module instead of a C app
  - If the program requires certain kernel options to be set ('mount' for example) then add exports.meta.kernelOptions = ["CONFIG_X"];
  - Remove it from the 'LEGACY INFO' listed below

>>> For every new format:
  - Remember that filename is now case sensitive. So if you want case insensitive use a /regex/i
  - Return as much 'input.meta' info about the file as I can including using 'extract' from media-libs/libextractor
  - Include at least 1 sample file in test/sample. Certainly include additional format samples that are 'edge cases' or different
  - For images, first convert with every image converter program and deduce the appropriate priority order based on those results
  - Try and validate whether the conversion worked as much as possible
  - Possibly set a 'probability' rating. Can test the entropy of the image (solid vs static vs actual image) or Audio could measure static vs melodic vs silence, etc. I should probably have this 'opt in' as it may be expensive to calculate
  - Animated cursors and other formats that produce a bunch of individual frame images should be converted to an animated GIF
  - Remove from formatsToDoASAP.txt

>>> Specific format notes:
	BSAVE: Consider introducing exports.meta.try : true and adding support for when dexvert is called with --try to also run it through any format marked as 'try' since the program chances so greatly based on the input
	ISO: iso-info
	GIF: whether it's animated or not
	GRASP: If it's some sort of powerpoint, maybe I should reclassify it as 'document' instead of 'archive'
	ANI: Ensure that the animated cursor is extracted out into an animated GIF, rather than individual frames of animation images

>>> Specific program notes:
	unoconv: Need a way to launch unoconv as a server process in the background. Will need to think about the best way to do that so that I can run multiple 'dexvert' commands CLI
	ancient: ADD this as a program and ensure it's included in the --try

* Add the unsupported formats. Mark them as exports.meta.unsupported : true. The notes I took in unsupported.txt should be added to exports.meta.notes = ``. Also add unsupported samples to test/sample/ and update the test suite to skip any formats that are 'unsupported'. Update the unsupported notes with the new info from magnus.bergman@snisurset.net on robert@telparia

* Create a util/buildSupported.js and a util/buildUnsupported.js that will create a SUPPORTED.md and UNSUPPORTED.md files that lists all the formats. Ensure I include the notes field too and a "5 sample files" hyperlink to the appropriate test/sample subdir

* Update archiveteam wiki and change all the sample file links to point to the github repo

* Add support for the new abydos images formats that are part of the new version of the abydos test suite and new abydos version



>>> LEGACY INFO, DELETE AS I GO!!!
>>> Here are the apps I unmerged
amigadepacker helpdeco jhove ppunpack rsrce sharutils tika xdms nrg2iso shed dosemu uade virtio-win wine-vanilla binwalk unoconv djview ghostpcl-bin grotag writerperfect ab2ascii amosbank amostools stackimport deark nconvert recoil abydos libbpg rtmidi adplay mikmod openmpt123 timidity++ vgmstream xmp xanim mpack xwud xautomation media-sound/fluid-soundfont media-sound/timidity-freepats ansilove netpbm physfs
>>> Here are DevLab/apps/* that I moved to legacy:
abydosconvert midstar2mid mikmodInfo mod2wav modInfo recordxvfb unmbox imageInfo
>>> I moved from compendium/bin to DevLab/legacy:
retrovert pcl2pdf unnrg unpcx* undirector wavduration
>>> I removed symlinks from compendium/bin:
mod2wav mikmodInfo midistar2mid modInfo unmbox imageInfo abydosconvert tridDefToMagic




/---------------\
| High Priority |
\---------------/


/-----------------\
| Medium Priority |
\-----------------/
* Create retrovert.com with a 'Browse' button that allows you to upload files including a REST API

* Create a 'dexvert-remote' NPM  package that will provide the same 'dexvert inputFilePath outputDirPath' command line tool but it will upload the file to the website and then download the results all seamlessly (with progress bar)

* Possible JS based amiga emulator: http://scriptedamigaemulator.net/
  Could be useful for better ADF extraction support or maybe some of the file formats I mentioned in unsupported.txt

* Create a copy of every single .instr and .ss file from every item into smus2wav/StandardInstruments (would have to check for naming collisions (where the files have different checksums) and potentially support that if there are a lot.
Then the conversion app would have a much higher chance at being able to convert the files.
I could then create a retryFiles.js that would find all items and files that have processed false that match a given criteria (magic probably) and the re-run processFile for that item (which would likely mean breaking processFile fun into its on file, that shouldn't be too hard.)

* Create a NodeJS based parser that can parse internet e-mail/news formats to verify that they are indeed valid files
  http://fileformats.archiveteam.org/wiki/Internet_e-mail_message_format
  Then figure out what to do with it. Do I introduce a way to "verify" "ignored" files and add that to text.js? I could then mark it in A.HANDLER.text.imf: verify : true, weak : false    or something like this?


/--------------\
| Low Priority |
\--------------/
* Convert bitmap fonts to more usable vector fonts by trying to "autotrace" the fonts, or utilize a pro tool like: https://www.fontlab.com/font-editor/bitfonter/   or   https://www.fontlab.com/font-converter/transtype/

* Create native C nodejs module for imagemagick/recoil and other libs for speed. Example existing image magick one: https://github.com/elad/node-imagemagick-native
